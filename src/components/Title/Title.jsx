/* eslint-disable react/prop-types */

const Title = ({ children }) => {
    return (
        <h2 className="my-5 text-3xl text-[#e51681] text-center font-bold">{children}</h2>
    );
};

export default Title;